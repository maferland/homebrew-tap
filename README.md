# Homebrew Tap

Homebrew formulae for [Poke](https://github.com/maferland/poke) — ephemeral reminders for Claude Code sessions.

## Install

```bash
brew tap maferland/tap
brew install poke-mcp        # MCP server + hooks (required)
brew install --cask poke     # Menu bar app (optional)
```

## Upgrade

```bash
brew upgrade poke-mcp
brew upgrade --cask poke
```
